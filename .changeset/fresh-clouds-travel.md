---
"effect-wide-event": minor
---

Remove the `effect-wide-event/v3` endpoint and Effect v3 support. The package now supports Effect v4 only. Applications that use the v3 endpoint must upgrade to Effect v4 and import from `effect-wide-event`.
